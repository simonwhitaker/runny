shell: /bin/bash
env:
  - FOO=fooster
  - BAR=barbara
commands:
  clean:
    run: |
      go clean ./...
      rm -rf dist
  install-goreleaser:
    if: "! command -v goreleaser"
    run: brew install goreleaser/tap/goreleaser
  release:
    needs:
      - clean
      - install-goreleaser
    run: |
      export GITHUB_TOKEN=$(gh auth token)
      goreleaser
  test:
    run: go test ./...
  test-env:
    env:
      - FOO=baz
    run: echo $FOO $BAR
